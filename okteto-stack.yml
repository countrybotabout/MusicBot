services:
  drone-vc-p:
    build: .
    environment:
      API_ID: $API_ID  
      API_HASH: $API_HASH  
      BOT_TOKEN: $BOT_TOKEN 
      ADMINS: $ADMINS 
      GROUP_ID: $GROUP_ID 
    resources:
      cpu: 1000m
      memory: 3Gi
      
      
